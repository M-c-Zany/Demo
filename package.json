{
  "devDependencies": {
    "cypress": "^13.3.0",
    "cypress-parallel": "^0.13.0"
  },
  "scripts": {
    "cypress:run": "cypress run --record --parallel --ci-build-id $(date +%s) 'cypress/e2e/**/*.cy.js'"
  }
}
